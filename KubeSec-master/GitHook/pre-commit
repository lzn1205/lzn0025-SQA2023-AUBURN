#!/bin/bash

# Run security scan on Python files about to be committed
while read -r file; do
    if [[ $file =~ \.py$ ]]; then
        python security_scanner.py "$file"
    fi
done < <(git diff --cached --name-only --diff-filter=ACM)